<li>
  Name: <%= user.name %>
  <span class="profile-link">
    <%= link_to 'See Profile',  user_path(user), class: 'profile-link' %>
  </span>
  <%= form_for(current_user.friendship, url: user_friendships_path(user))
  do |form|%>
      <%=form.submit 'Add Friend', class: ''%>
  <% end %>
</li>